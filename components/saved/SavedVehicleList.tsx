"use client";
import {vehicles} from "@/data/vehicles"; import {VehicleCard} from "@/components/vehicles/VehicleCard"; import {useSavedVehicles} from "@/hooks/useSavedVehicles"; import Link from "next/link";
export function SavedVehicleList(){const{ids}=useSavedVehicles();const saved=vehicles.filter(v=>ids.includes(v.id));return saved.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{saved.map(v=><VehicleCard key={v.id} vehicle={v}/>)}</div>:<div className="rounded-3xl bg-white p-12 text-center"><h2 className="text-2xl font-black">No saved vehicles yet</h2><p className="mt-2 text-slate-500">Tap the heart on any vehicle to keep it here.</p><Link href="/vehicles" className="mt-6 inline-flex rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">Browse vehicles</Link></div>}
